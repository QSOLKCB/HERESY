# HERESY v4.0.0 — The Modern Developer Simulator

The complete v4 root is preserved byte-for-byte in [`original/`](original/).
Its COBOL-defined fixed-record database, browser application, build scripts,
CI workflow, metadata, and documentation match the immutable `v4.0.0` tag.

Run its checks independently:

```sh
make -C original check
```

The shorter historical `editions/v4-modern-dev-sim/` directory remains in the
repository because it was already published. This full archive is the canonical
edition snapshot used by v5 verification.

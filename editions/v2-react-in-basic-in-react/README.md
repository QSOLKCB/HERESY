# HERESY v2.0.0 — React inside BASIC inside React

This directory preserves the complete v2 root edition before HERESY v3 replaced
it as the anthology's main offence.

The byte-for-byte historical snapshot is under [`original/`](original/). It is
still runnable:

```sh
cd original
npm ci
npm run dev
```

Complete verification:

```sh
npm run check
```

The snapshot contains the original source, locked dependency graph, CI workflow,
build configuration, citation metadata, manifesto and 1.44 MB size gate.

> “I rendered React through BASIC because direct component creation lacked
> theological depth.”
